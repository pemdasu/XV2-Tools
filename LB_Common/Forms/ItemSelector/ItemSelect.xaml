<mvvm:AutoObservableWindow x:Class="LB_Common.Forms.ItemSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mvvm="clr-namespace:LB_Common.Mvvm"
        xmlns:local="clr-namespace:LB_Common.Forms"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Item Selector" Height="Auto" SizeToContent="Height" Width="500" ShowInTaskbar="True" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowIconOnTitleBar="False" x:Name="Window" TitleCharacterCasing="Normal" Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="600"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="listBox" Style="{DynamicResource MahApps.Styles.DataGrid.Azure}" BorderBrush="{DynamicResource MahApps.Brushes.Accent2}" BorderThickness="0.5" Margin="20" AutoGenerateColumns="False" CanUserDeleteRows="False" CanUserAddRows="False" ItemsSource="{Binding ElementName=Window, Path=FilterList}" IsTextSearchEnabled="True" IsTextSearchCaseSensitive="False" TextSearch.TextPath="Name">
            <DataGrid.Columns>
                <DataGridTemplateColumn x:Name="imageColumn" Header="" Width="{Binding ElementName=Window, Path=BitmapColumnWidth}" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Bitmap}" Width="{Binding ElementName=Window, Path=BitmapWidth}" Height="{Binding ElementName=Window, Path=BitmapHeight}" Stretch="Uniform" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn x:Name="idColumn" Header="ID" Binding="{Binding ID}" IsReadOnly="True" Width="Auto"/>
                <DataGridTextColumn x:Name="preNameColumn" Header="PostName" Binding="{Binding PreNameString}" IsReadOnly="True" Width="Auto"/>
                <DataGridTemplateColumn x:Name="nameColumn" Header="Name" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" ToolTip="{Binding Name}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" ToolTip="{Binding Name}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn x:Name="postNameColumn" Header="PostName" Binding="{Binding PostNameString}" IsReadOnly="True" Width="Auto"/>
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding ElementName=Window, Path=SelectItemCommand}"/>
                <KeyBinding Key="Return" Command="{Binding ElementName=Window, Path=SelectItemCommand}"/>
            </DataGrid.InputBindings>
        </DataGrid>

        <TextBox Grid.Row="1" Text="{Binding ElementName=Window, Path=SearchFilter, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource MahApps.Styles.TextBox.Search}" Controls:TextBoxHelper.Watermark="Search..." Controls:TextBoxHelper.ButtonCommand="{Binding ElementName=Window, Path=ClearSearchCommand}" BorderBrush="{DynamicResource MahApps.Brushes.Accent2}" Controls:TextBoxHelper.ClearTextButton="True" Height="25" Width="300" TextWrapping="Wrap" />

        <CheckBox Grid.Row="2" x:Name="checkbox" ToolTip="{Binding ElementName=Window, Path=BooleanParameterToolTip}" IsChecked="{Binding ElementName=Window, Path=BooleanParameter}" FontWeight="SemiBold" Content="{Binding ElementName=Window, Path=BooleanParameterName}" Width="175" Margin="5" Visibility="Collapsed"/>

        <Button Grid.Row="3" Content="Ok" Height="30" Width="80" Command="{Binding ElementName=Window, Path=SelectItemCommand}" Margin="5"/>

    </Grid>
</mvvm:AutoObservableWindow>